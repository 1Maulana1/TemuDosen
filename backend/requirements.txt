django==5.2.*
djangorestframework==3.17.*
django-cors-headers==4.9.*
django-environ==0.14.*
django-filter==25.2
python-magic==0.4.27
google-api-python-client==2.*
google-auth-oauthlib==1.*
google-auth-httplib2==0.2.*
APScheduler==3.*
cryptography==44.*
reportlab==4.*
pytest==9.1.*
pytest-django==4.12.*
factory-boy==3.3.*

# Phase 6: STT + AI Summarization + Logbook (Celery/Redis broker, faster-whisper STT, Anthropic LLM)
faster-whisper==1.2.1
celery==5.6.3
# celery 5.6.3 has a documented Redis result-backend pubsub reconnect regression affecting
# redis-py < 5.3.0 (celery/celery#10294). Not exercised here — CELERY_RESULT_BACKEND is
# intentionally unset (ignore_result=True on all tasks, see below). Still: keep redis >= 5.3.0
# whenever celery==5.6.3 is pinned; do not let these two versions drift apart independently.
redis==8.0.1
anthropic>=0.69
pydantic>=2.7
