django==5.2.*
djangorestframework==3.17.*
django-cors-headers==4.9.*
django-environ==0.14.*
django-filter==25.2
python-magic==0.4.27; sys_platform != 'win32'
# Windows dev: python-magic needs a system libmagic that isn't there by default —
# importing it can crash with an OS-level access violation (not a catchable Python
# exception). python-magic-bin bundles a working libmagic.dll instead.
python-magic-bin==0.4.14; sys_platform == 'win32'
google-api-python-client==2.*
google-auth-oauthlib==1.*
google-auth-httplib2==0.2.*
APScheduler==3.*
cryptography==44.*
reportlab==4.*
pytest==9.1.*
pytest-django==4.12.*
factory-boy==3.3.*

# Phase 6: STT + AI Summarization + Logbook (Celery/Redis broker, Anthropic LLM).
# STT default via Groq Whisper API (free tier, hanya HTTP call — httpx sudah ikut
# sebagai dependency `anthropic`). faster-whisper = fallback provider 'local'.
faster-whisper==1.2.1
celery==5.6.3
# celery 5.6.3 punya regresi reconnect pubsub result-backend Redis untuk redis-py < 5.3.0
# (celery/celery#10294). Tidak terpicu di sini — CELERY_RESULT_BACKEND sengaja tidak diset
# (ignore_result=True di semua task). Tetap: jaga redis >= 5.3.0 selama celery==5.6.3 dipin.
redis==8.0.1
anthropic>=0.69
pydantic>=2.7
