---
phase: 01-submission-triage-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-04"]
files_modified:
  - backend/apps/submissions/serializers.py
  - backend/apps/submissions/views.py
  - backend/apps/submissions/urls.py
  - backend/apps/submissions/filters.py
  - backend/apps/submissions/tests/test_lecturer_view.py
  - frontend/src/pages/lecturer/LecturerDashboard.tsx
  - frontend/src/api/submissions.ts
  - frontend/src/pages/lecturer/LecturerDashboard.test.tsx
autonomous: false
requirements: [REVIEW-01]
must_haves:
  truths:
    - "A lecturer can view a list of guidance requests from their own advisee students, each showing NIM, name, symptom category, status, submitted date, and a draft preview link"
    - "A lecturer cannot see submissions belonging to other lecturers' students"
    - "The lecturer list can be filtered by status, searched by NIM or name, and sorted by submission date or symptom"
    - "Phase 1 shows view + PDF preview only — no approve/reject actions (deferred to Phase 2)"
  artifacts:
    - path: "backend/apps/submissions/views.py"
      provides: "LecturerSubmissionListView filtered to request.user's advisees"
      contains: "adviser"
    - path: "backend/apps/submissions/filters.py"
      provides: "SubmissionFilter (status filter) + search/ordering config"
      contains: "FilterSet"
    - path: "frontend/src/pages/lecturer/LecturerDashboard.tsx"
      provides: "Lecturer pending-requests dashboard S-08 (view-only, filter/search/sort)"
      contains: "Lihat Draft"
  key_links:
    - from: "frontend/src/pages/lecturer/LecturerDashboard.tsx"
      to: "/api/submissions/?status=&search="
      via: "fetch with filter/search/sort query params"
      pattern: "status="
    - from: "backend/apps/submissions/views.py"
      to: "Submission.student.adviser"
      via: "queryset filtered to submissions where student.adviser == request.user"
      pattern: "student__adviser"
---

<objective>
Deliver the lecturer review vertical slice: the lecturer-scoped submission list endpoint (returning
only the lecturer's own advisees' submissions) with status filtering, NIM/name search, and date/
symptom sorting, plus the lecturer dashboard UI (S-08) that lists pending requests with a draft
preview — view-only, with approve/reject deferred to Phase 2. After this plan a lecturer sees and
previews their advisees' submissions.

Purpose: Implements REVIEW-01 and D-09 (all statuses shown), D-10 (columns: NIM, name, symptom,
status, date, file + preview), D-11 (filter by status, search by NIM/name, sort by date/symptom),
D-12 (Phase 1 view + preview only; no approve/reject). Resolves the REVIEW-01 ownership-isolation
test (lecturer cannot see other lecturers' submissions) from VALIDATION.md.

Output: Lecturer-scoped, filterable submission list API + lecturer dashboard, tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-submission-triage-foundation/SKELETON.md
@.planning/phases/01-submission-triage-foundation/01-RESEARCH.md
@.planning/phases/01-submission-triage-foundation/01-CONTEXT.md
@.planning/phases/01-submission-triage-foundation/01-UI-SPEC.md
@.planning/phases/01-submission-triage-foundation/01-01-SUMMARY.md
@.planning/phases/01-submission-triage-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lecturer-scoped, filterable submission list API (REVIEW-01)</name>
  <files>backend/apps/submissions/serializers.py, backend/apps/submissions/views.py, backend/apps/submissions/urls.py, backend/apps/submissions/filters.py, backend/apps/submissions/tests/test_lecturer_view.py</files>
  <read_first>
    - .planning/phases/01-submission-triage-foundation/01-RESEARCH.md (REVIEW-01 row — ListAPIView filtered by lecturer FK + file_url to protected serve endpoint + django-filter; Don't Hand-Roll — django-filter)
    - backend/apps/submissions/models.py (Submission, SubmissionFile from Plan 04)
    - backend/apps/submissions/views.py (existing submission views from Plan 04 — extend, do not duplicate)
    - backend/apps/accounts/permissions.py (IsLecturer from Plan 01)
    - backend/apps/accounts/models.py (CustomUser.adviser / advisees relation from Plan 01)
    - .planning/phases/01-submission-triage-foundation/01-CONTEXT.md (D-09, D-10, D-11, D-12)
  </read_first>
  <behavior>
    - Test: a lecturer GET /api/submissions/ returns submissions ONLY for students whose adviser == that lecturer; never other lecturers' students (REVIEW-01 isolation).
    - Test: the serialized list includes student NIM, student name, symptom category names, status, created_at, original_filename, and a file_url pointing at /api/files/<uuid>/ (D-10).
    - Test: ?status=pending filters to pending submissions; all statuses are exposed (D-09).
    - Test: ?search=<nim-or-name> matches by student NIM or full_name (D-11).
    - Test: ?ordering=created_at and ?ordering=<symptom field> sort the list (D-11).
    - Test: a student or unapproved user hitting the lecturer list endpoint is rejected (403) — view-only lecturer scope.
  </behavior>
  <action>
    Add `filters.py` with a django-filter `SubmissionFilter` exposing a `status` filter, and configure
    the lecturer view with DjangoFilterBackend + SearchFilter (search_fields student__nim,
    student__full_name) + OrderingFilter (ordering_fields created_at and a symptom name annotation) per
    RESEARCH (Don't Hand-Roll django-filter). Add a `LecturerSubmissionSerializer` exposing the D-10
    columns: student_nim, student_name, symptom_names, status, created_at, original_filename, and
    file_url (built from the SubmissionFile uuid → /api/files/<uuid>/, NOT a media path). Extend
    views.py with `LecturerSubmissionListView` (ListAPIView, IsLecturer+IsApproved) whose get_queryset
    returns `Submission.objects.filter(student__adviser=self.request.user)` (REVIEW-01 isolation,
    D-12 read-only — no approve/reject actions in this phase). Route it under /api/submissions/ for the
    lecturer role (distinguish from the student create/list view by role in get_queryset, or add a
    dedicated path the dashboard calls) in submissions/urls.py. Write the behavior tests in
    test_lecturer_view.py (mirrors VALIDATION T-1-03-01 and the "cannot see others" isolation test).
    Do NOT add Approve/Reject endpoints — those are Phase 2 (D-12).
  </action>
  <verify>
    <automated>cd backend && pytest apps/submissions/tests/test_lecturer_view.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "student__adviser" backend/apps/submissions/views.py` >= 1 (advisee isolation).
    - `grep -c "FilterSet" backend/apps/submissions/filters.py` >= 1 (django-filter, not hand-rolled).
    - `grep -c "/api/files/" backend/apps/submissions/serializers.py` >= 1 (file_url via protected route).
    - test_lecturer_view.py asserts isolation (lecturer sees only own advisees), the D-10 columns, status filter, search, ordering, and 403 for non-lecturers; passes.
    - No approve/reject route added (Phase 2 scope) — confirmed by absence of "approve"/"reject" in submissions/urls.py beyond user-approval (which lives in accounts).
  </acceptance_criteria>
  <done>Lecturer-scoped, filterable/searchable/sortable submission list API implemented and tested with advisee isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Lecturer dashboard (S-08) — pending requests, filter/search, draft preview (view-only)</name>
  <files>frontend/src/pages/lecturer/LecturerDashboard.tsx, frontend/src/api/submissions.ts, frontend/src/pages/lecturer/LecturerDashboard.test.tsx</files>
  <read_first>
    - .planning/phases/01-submission-triage-foundation/01-UI-SPEC.md (S-08 lecturer dashboard — header, bottom nav, Permintaan Masuk list, filter tabs All|Menunggu|Disetujui|Revisi, search field, "Lihat Draft" only, empty state copy; Status Badge Contract; Accessibility Contract)
    - frontend/src/api/submissions.ts (fetchMySubmissions from Plan 04 — add fetchLecturerSubmissions)
    - frontend/src/components/StatusBadge.tsx (Plan 02), frontend/src/components/PDFPreview.tsx (Plan 04)
    - frontend/src/router.tsx (dosen/ placeholder from Plan 01)
    - E:\Proyek S4\stitch_temudosen_platform_dokumentasi_kontinuitas_bimbingan\dosen_dashboard_dr._rina_2\code.html
  </read_first>
  <action>
    Extend `api/submissions.ts` with `fetchLecturerSubmissions({status, search, ordering})` calling
    /api/submissions/ with query params via the Plan-01 apiRequest client. Implement LecturerDashboard
    (S-08): fixed header (avatar + "TemuDosen" wordmark + notification bell) + bottom nav
    (Beranda|Antrean|Riwayat|Profil), greeting heading, date line. "Permintaan Masuk" section renders a
    card per submission showing student avatar/name, NIM, symptom label, status via StatusBadge, and a
    draft file chip with size and a "Lihat Draft" button that opens the PDFPreview (Plan 04) against
    /api/files/<uuid>/. Filter bar: tabs All | Menunggu | Disetujui | Revisi (active:
    text-primary border-b-2 border-primary) driving the status query param; search field
    (placeholder "Cari NIM atau nama...") driving the search param. CRITICAL per D-12: render only
    "Lihat Draft" — the approve/reject buttons seen in the mockup are NOT included in Phase 1 (hidden).
    Empty state: heading "Belum Ada Permintaan Masuk", body "Mahasiswa bimbingan Anda belum mengajukan
    sesi bimbingan." All Tailwind tokens; 44x44px touch targets, focus-visible, 14px min font per the
    Accessibility Contract. Replace the router.tsx dosen/ placeholder. Add a Vitest test (MSW-mocked)
    asserting: the list renders submission cards with NIM + symptom + status badge, switching the status
    tab issues a request with the status param, the search field issues a search param, and NO
    approve/reject button is rendered (D-12).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Lihat Draft" frontend/src/pages/lecturer/LecturerDashboard.tsx` >= 1.
    - `grep -c "Belum Ada Permintaan Masuk" frontend/src/pages/lecturer/LecturerDashboard.tsx` >= 1 (empty state copy).
    - `grep -c "Cari NIM atau nama" frontend/src/pages/lecturer/LecturerDashboard.tsx` >= 1 (search placeholder).
    - No "Setujui"/"Tolak"/"Approve"/"Reject" action button in LecturerDashboard.tsx (D-12) — grep returns 0.
    - LecturerDashboard.test.tsx passes: cards render, status-tab + search drive query params, no approve/reject button present.
    - `npx vite build` succeeds.
  </acceptance_criteria>
  <done>Lecturer dashboard lists advisees' submissions with filter/search and draft preview, view-only per D-12; Vitest test green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The lecturer review dashboard: a filterable/searchable list of the lecturer's advisees' submissions with PDF preview, view-only (no approve/reject in Phase 1).</what-built>
  <how-to-verify>
    1. Ensure at least one approved student (assigned to lecturer A) has submitted a request (from Plan 04 flow). Optionally create a second lecturer B with their own advisee + submission.
    2. Log in as lecturer A; open /dosen/ (S-08) on a 360px viewport. Confirm the "Permintaan Masuk" list shows lecturer A's advisee's submission with NIM, name, symptom, a "Menunggu" badge, date, and a draft file chip (D-10).
    3. Confirm there are NO Approve/Reject buttons — only "Lihat Draft" (D-12). Click "Lihat Draft" and confirm the PDF renders.
    4. Use the status tabs (All|Menunggu|Disetujui|Revisi) and the "Cari NIM atau nama..." search; confirm the list filters/searches correctly (D-11).
    5. Log in as lecturer B and confirm B sees ONLY their own advisee's submission and NOT lecturer A's (REVIEW-01 isolation).
    6. Confirm the empty state "Belum Ada Permintaan Masuk" shows for a lecturer with no advisee submissions.
  </how-to-verify>
  <resume-signal>Type "approved" if the lecturer list, isolation, filter/search, and preview all work and no approve/reject appears, or describe what failed.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
- `apps.submissions.filters.SubmissionFilter` — status filter (+ search/ordering config on the view)
- `apps.submissions.serializers.LecturerSubmissionSerializer` — D-10 columns incl. file_url to protected route
- `apps.submissions.views.LecturerSubmissionListView` — advisee-scoped list (REVIEW-01 isolation)
- Endpoint: GET /api/submissions/ (lecturer role, advisee-scoped, ?status=&search=&ordering=)
- Frontend: api/submissions.ts fetchLecturerSubmissions; pages/lecturer/LecturerDashboard
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lecturer → GET /api/submissions/ | must only return the lecturer's own advisees' data |
| Non-lecturer → lecturer list | privilege boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-21 | Information Disclosure | Lecturer reads another lecturer's students' submissions | mitigate | get_queryset filters student__adviser=request.user; test asserts isolation (Task 1) |
| T-1-22 | Elevation of Privilege | Student/unapproved user hits lecturer list | mitigate | IsLecturer + IsApprovedUser; test asserts 403 (Task 1) |
| T-1-23 | Information Disclosure | file_url exposes a guessable/public path | mitigate | file_url points at the protected /api/files/<uuid>/ route which re-checks ownership (Plan 04); never a media path |
| T-1-24 | Information Disclosure | Search/filter query injection | mitigate | django-filter + DRF SearchFilter parameterize queries via the ORM (Task 1) |

All threats mitigated. No HIGH-severity unmitigated threat.
</threat_model>

<verification>
- `cd backend && pytest apps/submissions/tests/test_lecturer_view.py -q` green (isolation, columns, filter/search/sort, 403).
- `cd frontend && npx vitest run` green; `npx vite build` succeeds.
- Human checkpoint: lecturer sees only own advisees, filter/search/preview work, no approve/reject.
- Full phase gate: `cd backend && pytest -q` (all apps green) and `cd frontend && npx vitest run` before /gsd-verify-work.
</verification>

<success_criteria>
- Lecturer sees a filterable/searchable/sortable list of their own advisees' submissions with the D-10 columns and a working preview.
- Lecturer cannot see other lecturers' students' submissions.
- No approve/reject in Phase 1 (deferred to Phase 2).
</success_criteria>

<output>
Create `.planning/phases/01-submission-triage-foundation/01-05-SUMMARY.md` when done.
</output>
