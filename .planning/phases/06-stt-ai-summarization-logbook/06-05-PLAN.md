---
phase: 06-stt-ai-summarization-logbook
plan: 05
type: execute
wave: 3
depends_on: [06-03, 06-04]
files_modified:
  - backend/apps/logbook/tasks.py
  - backend/apps/bimbingan/views.py
  - backend/apps/logbook/tests/test_logbook_tasks.py
autonomous: true
requirements: [STT-01, STT-02, STT-03, STT-07, ADMIN-05]
user_setup:
  - service: anthropic
    why: "LLM summarization (STT-03). Absent by default — STT_LLM_ENABLED=False keeps the pipeline gracefully disabled until provisioned (D-04)."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com → Settings → API Keys (provisioned at deploy time; not required for this phase's tests)"
      - name: STT_LLM_ENABLED
        source: "Set to True in the deploy environment once the key exists; default False"

must_haves:
  truths:
    - "CompleteSessionView creates a SessionLogbook and fires transcribe_session.delay() on session completion (D-06), returning immediately (non-blocking, STT-02)"
    - "transcribe_session persists transcript + SessionRecording.duration_seconds, then triggers summarization; failure/timeout → status=failed + a distinct SystemLog event_type"
    - "poll_summary_batch persists the validated summary AND the actual token counts + computed IDR cost onto SessionLogbook at pipeline completion (D-11, batched/cached — never recomputed on dashboard load)"
    - "STT and LLM timeouts use separate clocks (2x duration/min-5min vs LLM_BATCH_TIMEOUT_MINUTES=180) and distinct event_types (D-08)"
  artifacts:
    - path: "backend/apps/logbook/tasks.py"
      provides: "transcribe_session, submit_summary_batch, poll_summary_batch Celery tasks + _fail helper"
      contains: "def transcribe_session"
  key_links:
    - from: "backend/apps/bimbingan/views.py CompleteSessionView"
      to: "backend/apps/logbook/tasks.py transcribe_session"
      via: "transcribe_session.delay(logbook.id) after SessionLogbook created"
      pattern: "transcribe_session.delay"
    - from: "backend/apps/logbook/tasks.py poll_summary_batch"
      to: "SessionLogbook token/cost fields"
      via: "compute_cost_idr + save llm_input_tokens/llm_output_tokens/llm_cost_estimate_idr"
      pattern: "llm_cost_estimate_idr"
---

<objective>
Author the full async Celery chain that turns a completed recording into a reviewable logbook: `transcribe_session` → `submit_summary_batch` → `poll_summary_batch`, wired to fire from `CompleteSessionView` via `.delay()` (D-06). This is where the two service wrappers (06-03) are orchestrated, where D-08's split-stage timeouts live, and where D-11's token/cost accumulation is persisted at pipeline completion — before the Wave-4 admin display sums it.

Purpose: This is the pipeline's spine. It must be non-blocking (STT-02), degrade gracefully to manual notes on any failure/timeout with a distinct `SystemLog.event_type` (STT-07, D-08), and record cost exactly once when tokens become available (D-11) so the dashboard never recomputes O(n) on load. tasks.py is authored entirely here — no cross-wave forward references.

Output: `apps/logbook/tasks.py` (the three chained tasks + `_fail` helper + a cheap groundedness cross-check flag), the `CompleteSessionView` `.delay()` trigger, and eager-mode integration tests.
</objective>

<artifacts_produced>
This plan produces:
- **`tasks.py`:** `transcribe_session(logbook_id)` (STT stage: guard flag, transcribe_audio, persist `transcript` + `SessionRecording.duration_seconds`, empty-transcript gate, status→`summarizing`, trigger `submit_summary_batch`; failure→`_fail` with `STT_FAILED`/`STT_TIMEOUT`/`STT_DISABLED`), `submit_summary_batch(logbook_id)` (submit batch-of-one, persist `batch_id`, schedule first poll; disabled→`LLM_DISABLED`), `poll_summary_batch(logbook_id)` (retrieve/parse, persist `summary_raw`, compute + persist `llm_input_tokens`/`llm_output_tokens`/`llm_cost_estimate_idr`, set groundedness flags on summary items, status→`ready_for_review`; failure→`LLM_FAILED`/`LLM_TIMEOUT`/`LLM_VALIDATION_FAILED`), `_fail(logbook, event_type)` helper
- **`CompleteSessionView` modification:** create `SessionLogbook(source_mode=session.method)` after DONE, then `transcribe_session.delay(logbook.id)`
- **Groundedness cross-check:** deterministic transcript-token check flagging summary items whose key noun-phrases are absent from the transcript (AI-SPEC §6, "Flag, do not block")
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-stt-ai-summarization-logbook/06-CONTEXT.md
@.planning/phases/06-stt-ai-summarization-logbook/06-RESEARCH.md
@.planning/phases/06-stt-ai-summarization-logbook/06-AI-SPEC.md
@.planning/phases/06-stt-ai-summarization-logbook/06-PATTERNS.md

# Live analogs:
@backend/apps/bimbingan/views.py
@backend/apps/bimbingan/scheduler.py
@backend/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: transcribe_session task + CompleteSessionView trigger</name>
  <files>backend/apps/logbook/tasks.py, backend/apps/bimbingan/views.py, backend/apps/logbook/tests/test_logbook_tasks.py</files>
  <read_first>
    - .planning/phases/06-stt-ai-summarization-logbook/06-RESEARCH.md (Pattern 2 tasks.py `transcribe_session` + `_fail` — the authoritative code; "faster-whisper duration capture" persisting duration_seconds; Pitfall 1 non-blocking; Pitfall 4 STT timeout is duration-derived not poll-based)
    - .planning/phases/06-stt-ai-summarization-logbook/06-CONTEXT.md (D-06 chain + state machine; D-08 STT stage = 2x duration min 5min; D-09 Celery native retry)
    - backend/apps/bimbingan/views.py (lines 761-836 CompleteSessionView — insert the SessionLogbook create + `.delay()` immediately after `session.save(...)`/recording save; mirror the existing non-blocking notify_student call shape but use `.delay()`)
    - backend/apps/logbook/services/stt.py (transcribe_audio — the callable this task invokes)
  </read_first>
  <behavior>
    - With `CELERY_TASK_ALWAYS_EAGER=True` and a mocked `transcribe_audio` returning ("teks", 12.0): `transcribe_session(logbook_id)` sets `transcript`, `SessionRecording.duration_seconds == 12.0`, `status == 'summarizing'`, and enqueues `submit_summary_batch`.
    - `STT_LLM_ENABLED=False` → status `failed`, `SystemLog(event_type='STT_DISABLED')`, no transcribe call.
    - A raised exception in `transcribe_audio` → status `failed`, `SystemLog(event_type='STT_FAILED')`.
    - An empty/blank transcript → status `failed` (empty-transcript gate, skip LLM), distinct log.
    - `CompleteSessionView` creates exactly one `SessionLogbook` per completed session with `source_mode` matching `session.method`, and returns HTTP 200 without waiting on transcription.
  </behavior>
  <action>
    Create `tasks.py` with `@shared_task(bind=True, autoretry_for=(Exception,), retry_backoff=True, max_retries=2, ignore_result=True) transcribe_session(self, logbook_id)` following 06-RESEARCH.md Pattern 2: load `SessionLogbook.objects.select_related('session__recording').get(id=logbook_id)`; if not `settings.STT_LLM_ENABLED` → `_fail(logbook, 'STT_DISABLED')` return; wrap `transcribe_audio(recording.file_path)` in try/except → on exception `SystemLog` + `_fail(logbook, 'STT_FAILED')`; enforce the STT-stage timeout as a duration-derived guard (2× audio duration, min 5 minutes) and log `STT_TIMEOUT` distinctly if exceeded; persist `recording.duration_seconds` (D-02) and `logbook.transcript`; if transcript is blank → `_fail(logbook, 'STT_EMPTY')` (skip LLM, AI-SPEC §6 empty-transcript gate); else set `status='summarizing'`, save, and `submit_summary_batch.delay(logbook.id)`. Add `_fail(logbook, event_type)` helper (status='failed', save, SystemLog ERROR). In `CompleteSessionView.post` (bimbingan/views.py), after the existing `session.save(...)`/recording handling, create `SessionLogbook.objects.create(session=session, source_mode=session.method or 'offline')` and call `transcribe_session.delay(logbook.id)` — fire-and-return, do NOT await (the NFR-01 blocking anti-pattern must not be repeated). Add eager-mode tests in `test_logbook_tasks.py` for each behavior above (patch `transcribe_audio` and `submit_summary_batch`).
  </action>
  <verify>
    <automated>cd backend && pytest apps/logbook/tests/test_logbook_tasks.py -q -k "transcribe or complete or duration or disabled"</automated>
  </verify>
  <acceptance_criteria>
    - Tests pass: success path sets transcript+duration+summarizing+enqueue; disabled/failed/empty paths set status=failed with the correct distinct event_types.
    - `grep -c "transcribe_session.delay" backend/apps/bimbingan/views.py` is 1 (trigger present, non-blocking).
    - `grep -c "STT_TIMEOUT\|STT_FAILED\|STT_DISABLED" backend/apps/logbook/tasks.py` is ≥3 (distinct STT event_types).
    - CompleteSessionView still returns 200 in its existing tests (no blocking regression) — run `pytest apps/bimbingan/tests/test_session_execution.py -q`.
  </acceptance_criteria>
  <done>Session completion fires an async transcription that persists transcript+duration and chains to summarization, with graceful, distinctly-logged failure paths and a non-blocking view.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: submit_summary_batch + poll_summary_batch with token/cost accumulation (D-11) + groundedness flag</name>
  <files>backend/apps/logbook/tasks.py, backend/apps/logbook/tests/test_logbook_tasks.py</files>
  <read_first>
    - .planning/phases/06-stt-ai-summarization-logbook/06-AI-SPEC.md (Section 4 Core Pattern — the authoritative submit_summary_batch/poll_summary_batch code, custom_id matching, validation-failure path; Section 6 groundedness cross-check "Flag, do not block"; Section 7 the D-08 timeout tension)
    - .planning/phases/06-stt-ai-summarization-logbook/06-CONTEXT.md (D-08 LLM stage = LLM_BATCH_TIMEOUT_MINUTES=180 decoupled from audio; D-11 token/cost persisted at completion, batched/cached, never real-time)
    - backend/apps/logbook/services/summarizer.py (build_batch_request, submit_batch, parse_result, compute_cost_idr — the callables this task orchestrates)
  </read_first>
  <behavior>
    - `submit_summary_batch(logbook_id)` (mocked client): persists `batch_id` BEFORE any poll, sets status `summarizing`, schedules `poll_summary_batch` (countdown). Disabled/no-key → `_fail(logbook, 'LLM_DISABLED')`.
    - `poll_summary_batch(logbook_id)` when batch `ended` + succeeded: persists `summary_raw` from the validated SessionSummary, persists `llm_input_tokens`/`llm_output_tokens` from the result usage and `llm_cost_estimate_idr = compute_cost_idr(...)`, sets `status='ready_for_review'`.
    - Batch still in progress → re-queues via `self.retry(countdown=60)`; `max_retries` derived from `LLM_BATCH_TIMEOUT_MINUTES` (180 → 180 retries at 60s), NOT a hardcoded ~1h ceiling (D-08/Pitfall 4). Exceeding it → `_fail(logbook, 'LLM_TIMEOUT')` (distinct from LLM_FAILED).
    - A non-succeeded result → `_fail(logbook, 'LLM_FAILED')`; a Pydantic ValidationError → SystemLog `LLM_VALIDATION_FAILED` + one re-submit before failing.
    - Groundedness: summary items whose key noun-phrases/chapter-refs/proper-nouns are absent verbatim from the transcript get a `grounded: false` flag in the persisted `summary_raw` (deterministic, no extra LLM call) — the UI reads this to render the "Perlu Verifikasi" chip. Flags never block persistence.
  </behavior>
  <action>
    Append `submit_summary_batch` and `poll_summary_batch` to `tasks.py` per 06-AI-SPEC.md Section 4. `submit_summary_batch(self, logbook_id)` (`@shared_task(bind=True, autoretry_for=(anthropic.APIConnectionError,), retry_backoff=True, max_retries=3, ignore_result=True)`): guard `_llm_enabled()`/key → `_fail(..., 'LLM_DISABLED')`; call `submit_batch`, persist `batch_id` + status `summarizing` BEFORE polling (crash-resume, State Management), then `poll_summary_batch.apply_async(args=[logbook_id], countdown=60)`. `poll_summary_batch(self, logbook_id)` (`@shared_task(bind=True, max_retries=settings.LLM_BATCH_TIMEOUT_MINUTES, ignore_result=True)`): retrieve batch by stored `batch_id`; if `processing_status != 'ended'` → `raise self.retry(countdown=60)` (retry exhaustion → `_fail(..., 'LLM_TIMEOUT')`); iterate results matching `custom_id == f'logbook-{logbook_id}'`; on non-succeeded → `_fail(..., 'LLM_FAILED')`; on succeeded → `parse_result`; on ValidationError → SystemLog `LLM_VALIDATION_FAILED` + one re-submit via `submit_summary_batch.delay` then `_fail` if it recurs; on success persist `summary_raw` (with per-item `grounded` flags from a `_flag_ungrounded(summary, transcript)` deterministic helper), `llm_input_tokens`/`llm_output_tokens` from the result's usage, `llm_cost_estimate_idr = compute_cost_idr(in, out)` (D-11 — computed here, once, at completion), and `status='ready_for_review'` via `save(update_fields=[...])`. Add `_flag_ungrounded` (cheap noun-phrase presence check against transcript text). Add eager-mode tests: token/cost persisted on completion, cost is the compute_cost_idr value (not recomputed elsewhere), LLM_TIMEOUT vs LLM_FAILED distinct, validation-failure path, groundedness flag set on an unsupported item.
  </action>
  <verify>
    <automated>cd backend && pytest apps/logbook/tests/test_logbook_tasks.py -q -k "summar or poll or token or cost or timeout or grounded or valid"</automated>
  </verify>
  <acceptance_criteria>
    - Tests pass: successful poll persists summary + token counts + IDR cost + ready_for_review; timeout→LLM_TIMEOUT; failure→LLM_FAILED; validation→LLM_VALIDATION_FAILED + one retry; groundedness flag set on an unsupported item.
    - `grep -c "llm_input_tokens\|llm_output_tokens\|llm_cost_estimate_idr" backend/apps/logbook/tasks.py` is ≥3 (D-11 accumulation lands here, before Wave-4 display).
    - `grep -c "LLM_BATCH_TIMEOUT_MINUTES" backend/apps/logbook/tasks.py` is ≥1 (retry ceiling derived from the env var, not hardcoded ~1h).
    - `grep -c "LLM_TIMEOUT" backend/apps/logbook/tasks.py` ≥1 AND `grep -c "LLM_FAILED" backend/apps/logbook/tasks.py` ≥1 (distinct clocks, D-08).
  </acceptance_criteria>
  <done>The LLM stage submits a batch-of-one, resumes safely across restarts, persists the validated summary with per-item groundedness flags, records actual token usage + IDR cost exactly once at completion (D-11), and distinguishes slow-timeout from genuine failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Celery task ↔ Redis broker | Task args (logbook_id) traverse the broker |
| worker → audio file | Uploaded audio decoded by the STT stage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering | logbook_id passed via broker | mitigate | Tasks defensively re-fetch `SessionLogbook`/`Session` and verify consistency before acting; Redis is network-isolated (T-06-01) |
| T-06-12 | Denial of Service | oversized/corrupt audio reaching transcribe_audio | mitigate | Upstream Phase-5 size/MIME validation (`RECORDING_MAX_UPLOAD_SIZE`) is the gate; the task re-checks the recording exists and is consistent, not re-implementing validation |
| T-06-13 | Repudiation | silent pipeline failure | mitigate | Every failure/timeout writes a distinct-`event_type` SystemLog row (STT_FAILED/STT_TIMEOUT/STT_EMPTY/STT_DISABLED/LLM_FAILED/LLM_TIMEOUT/LLM_VALIDATION_FAILED/LLM_DISABLED) feeding ADMIN-05 monitoring |
| T-06-14 | Information Disclosure | validation-failure log storing raw model input | accept | AI-SPEC §4 stores raw `tool_block.input` in SystemLog.context for debugging; content is already the transcript-derived summary, same sensitivity tier as SessionLogbook, access-controlled admin-only |
</threat_model>

<verification>
- Full chain runs eagerly in tests: complete → transcribe → summarize → ready_for_review.
- Token/cost persisted once at completion; STT and LLM timeouts are separate clocks with distinct event_types.
- CompleteSessionView is non-blocking; existing Phase-5 tests still green.
</verification>

<success_criteria>
The async pipeline transcribes and summarizes without blocking the request cycle, records D-11 token/cost at completion (ready for the Wave-4 dashboard to sum), flags ungrounded summary items for the reviewer, and falls back to a distinctly-logged failed state on any stage error or timeout — satisfying STT-01/02/03/07 and the ADMIN-05 data source.
</success_criteria>

<output>
Create `.planning/phases/06-stt-ai-summarization-logbook/06-05-SUMMARY.md` when done.
</output>
