---
phase: 06-stt-ai-summarization-logbook
plan: 08
type: execute
wave: 5
depends_on: [06-06]
files_modified:
  - frontend/package.json
  - frontend/src/components/video/VideoProvider.tsx
  - frontend/src/components/video/JitsiVideoProvider.tsx
  - frontend/src/components/video/JitsiVideoProvider.test.tsx
  - frontend/src/test/setup.ts
  - frontend/src/pages/lecturer/LecturerDashboard.tsx
  - frontend/src/pages/student/StudentQueue.tsx
  - frontend/src/pages/lecturer/LecturerQueue.tsx
  - frontend/src/pages/lecturer/LecturerRequests.tsx
autonomous: true
requirements: [VIDEO-01, VIDEO-02]

must_haves:
  truths:
    - "Online sessions render an embedded Jitsi call via a VideoProvider abstraction (D-13) with JitsiVideoProvider as the only implementation (VIDEO-01)"
    - "The lecturer's 'Sesi Berlangsung' card embeds the video BETWEEN the header row and the notes textarea, leaving the recording badge and Selesai flow untouched (D-17, S-16)"
    - "The student's queue screen gains an active-session card that shows the same video container but no recording/consent/Selesai controls (S-16)"
    - "The recording path for online sessions is the existing lecturer-mic-only useMediaRecorder flow — no new audio-capture code (VIDEO-02, D-16 cancelled/D-17 default)"
    - "The STT pipeline (apps/logbook/tasks.py) is not touched by any Jitsi work (D-14 hard boundary)"
  artifacts:
    - path: "frontend/src/components/video/VideoProvider.tsx"
      provides: "VideoProvider abstraction interface (D-13)"
      contains: "VideoProvider"
    - path: "frontend/src/components/video/JitsiVideoProvider.tsx"
      provides: "Jitsi implementation via @jitsi/react-sdk JitsiMeeting"
      contains: "JitsiMeeting"
  key_links:
    - from: "frontend/src/pages/lecturer/LecturerDashboard.tsx"
      to: "frontend/src/components/video/JitsiVideoProvider.tsx"
      via: "rendered inside the active-session card when method === 'online'"
      pattern: "JitsiVideoProvider"
---

<objective>
Add in-app embedded video for online sessions (VIDEO-01) via a `VideoProvider` abstraction (D-13) with a single `JitsiVideoProvider` implementation using `@jitsi/react-sdk`, embedded in both the lecturer's and student's active-session cards (S-16). This wave is standalone and comes AFTER the core pipeline works for offline sessions — it is deliberately not parallel with earlier waves. Per D-14, none of this touches the STT pipeline; per D-17, the recording path stays lecturer-mic-only via the existing `useMediaRecorder` flow (VIDEO-02 — no new capture code, D-16 dual-audio mixing was cancelled as not implementable against the locked iframe stack).

Purpose: Replace the external meeting-link click-through with an embedded call, while proving VIDEO-02 needs zero new audio work — the same recording that feeds STT for offline sessions feeds it for online sessions.

Output: `VideoProvider` + `JitsiVideoProvider` (+ test + jsdom mock), the lecturer active-session card embed, the new student active-session card, and the replacement of the external-link affordance in the active-session context — with the recording badge/Selesai flow untouched.
</objective>

<artifacts_produced>
This plan produces:
- **`@jitsi/react-sdk@1.4.4`** dependency in `frontend/package.json`
- **`VideoProvider.tsx`:** the D-13 abstraction (a props interface: `roomName`, `displayName`, `onApiReady?`; renders whichever provider implementation)
- **`JitsiVideoProvider.tsx`:** `@jitsi/react-sdk` `JitsiMeeting` at `domain="meet.jit.si"`, `configOverwrite={{ prejoinPageEnabled: false }}`, loading + error("Coba Lagi") overlays per S-16, `aspect-video rounded-xl ring-1 ring-primary/20` container
- **`JitsiVideoProvider.test.tsx`** + a `JitsiMeeting` jsdom mock in `frontend/src/test/setup.ts`
- **Modifications:** `LecturerDashboard.tsx` (embed between header and textarea when `activeSession.method === 'online'`), `StudentQueue.tsx` (new active-session card with the video container, read-only-of-controls), `LecturerQueue.tsx`/`LecturerRequests.tsx` (replace the external-link `<a>` in the active-session context only)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-stt-ai-summarization-logbook/06-UI-SPEC.md
@.planning/phases/06-stt-ai-summarization-logbook/06-RESEARCH.md
@.planning/phases/06-stt-ai-summarization-logbook/06-CONTEXT.md

# Live analogs:
@frontend/src/pages/lecturer/LecturerDashboard.tsx
@frontend/src/api/sessions.ts
@frontend/src/test/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @jitsi/react-sdk, build VideoProvider + JitsiVideoProvider + jsdom mock</name>
  <files>frontend/package.json, frontend/src/components/video/VideoProvider.tsx, frontend/src/components/video/JitsiVideoProvider.tsx, frontend/src/components/video/JitsiVideoProvider.test.tsx, frontend/src/test/setup.ts</files>
  <read_first>
    - .planning/phases/06-stt-ai-summarization-logbook/06-RESEARCH.md (Pattern 3 "Jitsi Meet External API" — the authoritative JitsiVideoProvider code, `@jitsi/react-sdk` `JitsiMeeting` usage, domain/configOverwrite/getIFrameRef; Pitfall 8 — the External API exposes NO remote track, so no dual-audio mixing is attempted here)
    - .planning/phases/06-stt-ai-summarization-logbook/06-UI-SPEC.md (S-16 visual contract — container classes, loading state "Menghubungkan panggilan video…", error state "Panggilan video gagal dimuat." + "Coba Lagi", `role="group" aria-label` wrapper, `prejoinPageEnabled:false` means auto-join, no "Bergabung" button)
    - frontend/src/test/setup.ts (full — the existing MediaRecorder/getUserMedia jsdom shim; add an analogous mock for `@jitsi/react-sdk`'s JitsiMeeting since a real Jitsi iframe cannot mount in jsdom, per 06-VALIDATION.md Wave 0)
  </read_first>
  <action>
    Add `@jitsi/react-sdk@1.4.4` to `frontend/package.json` dependencies ([OK] legitimacy verdict in 06-RESEARCH.md — no checkpoint needed). Create `components/video/VideoProvider.tsx` as the D-13 abstraction: a typed props interface (`roomName: string`, `displayName: string`, `onApiReady?: (api: unknown) => void`) and a component that delegates to the Jitsi implementation (structured so a future provider could be swapped without touching call sites). Create `components/video/JitsiVideoProvider.tsx` per 06-RESEARCH.md Pattern 3: `<JitsiMeeting domain="meet.jit.si" roomName={roomName} configOverwrite={{ startWithAudioMuted: false, prejoinPageEnabled: false }} userInfo={{ displayName }} onApiReady={...} getIFrameRef={(el)=>{el.style.height='100%'}} />` wrapped in the S-16 container (`w-full aspect-video rounded-xl overflow-hidden bg-slate-900 ring-1 ring-primary/20 relative`, `role="group" aria-label="Panggilan video sesi bimbingan"`), with a loading overlay (spinner + "Menghubungkan panggilan video…") shown until `onApiReady`, and an error overlay ("Panggilan video gagal dimuat." + a "Coba Lagi" button that remounts the component). Add a `JitsiMeeting` mock to `frontend/src/test/setup.ts` (renders a stub div, invokes `onApiReady` synchronously) mirroring the existing MediaRecorder shim. Add `JitsiVideoProvider.test.tsx`: asserts the container mounts with the expected `roomName`/`domain`, renders the loading state, and transitions past loading when the mocked `onApiReady` fires.
  </action>
  <verify>
    <automated>cd frontend && npm install && npm run test -- --run JitsiVideoProvider</automated>
  </verify>
  <acceptance_criteria>
    - `@jitsi/react-sdk` is in `package.json` and installs.
    - `JitsiVideoProvider.test.tsx` passes with the jsdom mock (mounts with roomName/domain, loading→ready).
    - `grep -c "prejoinPageEnabled" frontend/src/components/video/JitsiVideoProvider.tsx` is ≥1 (auto-join, no "Bergabung" button).
    - `grep -c "meet.jit.si" frontend/src/components/video/JitsiVideoProvider.tsx` is ≥1 (D-12 public instance).
    - No dual-audio mixing code: `grep -ci "AudioContext\|getOriginalStream\|MediaStreamAudioSource" frontend/src/components/video/JitsiVideoProvider.tsx` is 0 (D-16 cancelled).
  </acceptance_criteria>
  <done>A VideoProvider abstraction with a tested Jitsi implementation embeds a public-instance call with correct loading/error UX and no attempt at unreachable remote-track mixing.</done>
</task>

<task type="auto">
  <name>Task 2: Embed video in lecturer + student active-session cards; replace external-link affordance (recording path untouched)</name>
  <files>frontend/src/pages/lecturer/LecturerDashboard.tsx, frontend/src/pages/student/StudentQueue.tsx, frontend/src/pages/lecturer/LecturerQueue.tsx, frontend/src/pages/lecturer/LecturerRequests.tsx</files>
  <read_first>
    - frontend/src/pages/lecturer/LecturerDashboard.tsx (the "Sesi Berlangsung" active-session card — the header row with student name + "Merekam…" chip, the notes textarea, the "Selesai" button, and the `recorder.isRecording`/`useMediaRecorder` usage that must NOT change)
    - .planning/phases/06-stt-ai-summarization-logbook/06-UI-SPEC.md (S-16 — insert the video container BETWEEN the header row and the textarea, only when `method === 'online'`; student side gets the video container but NEVER the recording-consent controls, notes textarea, or Selesai button; the external-link `<a>` is superseded only within the active-session context, the queue-list "Online" chip stays)
    - .planning/phases/06-stt-ai-summarization-logbook/06-CONTEXT.md (D-14 pipeline stays video-agnostic — no backend/task changes; D-17 lecturer-mic-only recording unchanged for online = VIDEO-02; D-18 offline unaffected)
    - frontend/src/api/sessions.ts (ActiveSession type — `method`, `mahasiswa_name`; StudentQueueSession for the student card)
  </read_first>
  <action>
    In `LecturerDashboard.tsx`, when `queue.activeSession.method === 'online'`, render `<JitsiVideoProvider roomName={`temudosen-session-${activeSession.id}`} displayName={lecturerName} />` BETWEEN the card's header row and the `<textarea>` — derive `roomName` client-side (no backend change, keeps D-14 boundary). Do NOT modify `recorder.isRecording`, the "Merekam…" badge, the notes textarea, or `handleComplete`/"Selesai" (D-17: recording path is identical for online/offline; this satisfies VIDEO-02 with no new capture code). In `StudentQueue.tsx`, add a new active-session card (none exists today) that mirrors the lecturer card's video container when the student's session is `in_progress` and `method === 'online'` — student joins the call via the same `roomName` but the card shows NO consent controls, NO notes textarea, and NO "Selesai" button. In `LecturerQueue.tsx` and `LecturerRequests.tsx`, replace the external meeting-link `<a>` affordance within the active-session context only (leave the queue-list "Online" method chip as-is).
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run test -- --run && \
# Invariant baseline (pra-Jitsi, verified 2026-07-05): LecturerDashboard.tsx has
# useMediaRecorder x2 (import + hook call), recorder.isRecording x1, "Merekam" x1.
# These counts must stay EXACT after this task's edit (Task 2 only inserts
# JitsiVideoProvider between the header and textarea — it must not touch these
# lines). A count mismatch means the recording flow was accidentally
# modified/removed, not that the check is flaky.
grep -c "useMediaRecorder" src/pages/lecturer/LecturerDashboard.tsx | grep -qx "2" && \
grep -c "recorder.isRecording" src/pages/lecturer/LecturerDashboard.tsx | grep -qx "1" && \
grep -c "Merekam" src/pages/lecturer/LecturerDashboard.tsx | grep -qx "1" && \
echo "RECORDING_SYMBOLS_INTACT"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds and the full frontend suite passes (no regression to existing LecturerDashboard/useMediaRecorder tests).
    - `grep -c "JitsiVideoProvider" frontend/src/pages/lecturer/LecturerDashboard.tsx` is ≥1 and `grep -c "JitsiVideoProvider" frontend/src/pages/student/StudentQueue.tsx` is ≥1 (both sides embed).
    - `RECORDING_SYMBOLS_INTACT` printed by the automated verify command above — an exact-count invariant on `useMediaRecorder`/`recorder.isRecording`/`Merekam` in `LecturerDashboard.tsx`, baked from the actual pre-Jitsi baseline (2/1/1) rather than a "compare to before" note a reviewer would need to remember. Scoped to `LecturerDashboard.tsx` only: `StudentQueue.tsx` has zero recording symbols today (verified) since mic capture is lecturer-only (D-15/D-17) — there is nothing to protect on the student side.
    - D-14 boundary held: no file under `backend/apps/logbook/` is in this plan's diff.
  </acceptance_criteria>
  <done>Both parties get an embedded video call for online sessions; the lecturer-mic-only recording flow that feeds STT is unchanged (VIDEO-02 via D-17); offline sessions and the STT pipeline are untouched (D-14/D-18).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app ↔ meet.jit.si iframe | Cross-origin third-party call handling sensitive 1:1 advising audio/video |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-19 | Information Disclosure | meet.jit.si public instance, no SLA/security guarantee | accept | Recorded as a known MVP/demo-only limitation in PROJECT.md v2.3 (D-12); self-hosted/JaaS required before production — tracked, not a new finding |
| T-06-20 | Tampering | predictable roomName allowing an uninvited join | accept | `temudosen-session-{id}` is low-value for a demo; documented alongside the meet.jit.si limitation; hardening (JaaS auth) belongs to the deferred production-Jitsi work (D-19), not this phase |
| T-06-21 | Spoofing | iframe error masking a real capture failure | mitigate | The video error/"Coba Lagi" state is distinct from the mic-only `useMediaRecorder` permission flow (S-16) — a video failure does not silently break recording |
</threat_model>

<verification>
- JitsiVideoProvider mounts/tests green with a jsdom mock; no dual-audio mixing code exists.
- Both active-session cards embed the call; recording path and STT pipeline untouched.
- Full frontend suite green (no regression).
</verification>

<success_criteria>
Online sessions run through an embedded Jitsi call on both sides (VIDEO-01) behind a swappable VideoProvider abstraction, while VIDEO-02 is satisfied by the unchanged lecturer-mic-only recording flow feeding the same STT pipeline — with the D-14 pipeline boundary and D-16 cancellation both respected.
</success_criteria>

<output>
Create `.planning/phases/06-stt-ai-summarization-logbook/06-08-SUMMARY.md` when done.
</output>
