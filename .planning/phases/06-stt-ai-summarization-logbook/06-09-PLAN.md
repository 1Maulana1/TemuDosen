---
phase: 06-stt-ai-summarization-logbook
plan: 09
type: execute
wave: 6
depends_on: [06-05, 06-08]
files_modified:
  - backend/apps/logbook/tests/test_logbook_integration.py
  - backend/apps/bimbingan/tests/test_session_recording.py
autonomous: true
requirements: [STT-01, STT-02, STT-03, STT-04, STT-05, STT-06, STT-07, VIDEO-02]

must_haves:
  truths:
    - "An offline completed session runs the full pipeline end-to-end (complete → transcribe → summarize → ready_for_review → approve → student-visible) in eager mode with mocked STT/LLM"
    - "An online completed session records via the existing lecturer-mic-only flow and feeds the identical pipeline (VIDEO-02, D-17)"
    - "With STT_LLM_ENABLED=False, a completed session degrades cleanly to the manual-notes fallback and logs the disabled event (STT-07 graceful degradation)"
  artifacts:
    - path: "backend/apps/logbook/tests/test_logbook_integration.py"
      provides: "end-to-end pipeline tests (offline + online + disabled-path)"
      contains: "def test_"
  key_links:
    - from: "test_logbook_integration.py"
      to: "the full Wave 2-5 pipeline"
      via: "CompleteSessionView → transcribe_session → poll_summary_batch → ApproveLogbookView → StudentLogbookView"
      pattern: "ready_for_review"
---

<objective>
Prove the whole phase works end-to-end: an offline session and an online (lecturer-mic-only, D-17) session both flow from "Selesai" through transcription, summarization, lecturer approval, and student visibility; and a `STT_LLM_ENABLED=False` session degrades cleanly to manual notes (STT-07). This is test-only, runs in eager Celery mode with mocked STT/LLM, and depends on everything before it.

Purpose: The per-plan tests verify units; this wave verifies the seams between them — the exact place bugs hide. It also confirms VIDEO-02's central claim (online records via the same flow, no new capture code) and the graceful-degradation guarantee that makes "no API key yet" a safe default.

Output: `test_logbook_integration.py` (offline happy path, online happy path, disabled-fallback path) and an online-recording test in `test_session_recording.py`.
</objective>

<artifacts_produced>
This plan produces:
- **`test_logbook_integration.py`:** `test_offline_session_full_pipeline` (complete → ready_for_review → approve → student sees approved summary), `test_online_session_full_pipeline` (source_mode online, same pipeline), `test_disabled_pipeline_falls_back_to_manual` (STT_LLM_ENABLED=False → failed → manual-notes → approved-equivalent), `test_stt_failure_logs_and_falls_back` (corrupt audio → STT_FAILED + manual path available)
- **`test_session_recording.py` addition:** `test_online_session_lecturer_mic_only_recording` — an online session's recording upload succeeds via the unchanged useMediaRecorder/CompleteSessionView flow and produces the same `SessionRecording` → pipeline entry as offline (VIDEO-02, D-17)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-stt-ai-summarization-logbook/06-VALIDATION.md
@.planning/phases/06-stt-ai-summarization-logbook/06-CONTEXT.md

# Live analogs:
@backend/apps/bimbingan/tests/test_session_execution.py
@backend/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end pipeline integration tests (offline, online, disabled fallback)</name>
  <files>backend/apps/logbook/tests/test_logbook_integration.py</files>
  <read_first>
    - .planning/phases/06-stt-ai-summarization-logbook/06-VALIDATION.md (Per-Task Verification Map — the exact behaviors and requirement mapping this wave closes; Wave 0 Requirements — eager mode + mocked WhisperModel discipline)
    - backend/apps/bimbingan/tests/test_session_execution.py (the CompleteSessionView test setup — session in IN_PROGRESS with consent, multipart audio upload, webm_audio_file fixture — reuse this to drive completion)
    - backend/conftest.py (advisee_student, lecturer_user, authenticated_lecturer/advisee/student, webm_audio_file fixtures)
    - .planning/phases/06-stt-ai-summarization-logbook/06-CONTEXT.md (D-06 state machine; STT-07 fallback; D-17 online = lecturer-mic-only)
  </read_first>
  <behavior>
    - Offline: an IN_PROGRESS consented session, completed with a mock audio upload (STT_LLM_ENABLED=True, `transcribe_audio` + Anthropic client mocked) → SessionLogbook reaches `ready_for_review` with a summary; the owning lecturer approves; the owning student's `/api/logbook/student/<id>/` returns the approved summary; a non-owner student gets 403/404.
    - Online: same flow with `session.method='online'` → logbook `source_mode == 'online'`, pipeline identical, student sees approved result.
    - Disabled: `STT_LLM_ENABLED=False` → completing the session leaves the logbook `failed` with a `SystemLog(STT_DISABLED)`; the lecturer's manual-notes POST flips it to approved-equivalent and the student then sees it (STT-07 end-to-end).
    - STT failure: `transcribe_audio` raising → `failed` + `SystemLog(STT_FAILED)`, and the manual-notes path is available.
  </behavior>
  <action>
    Create `test_logbook_integration.py` running under eager Celery (test settings already set `CELERY_TASK_ALWAYS_EAGER=True`). Patch `apps.logbook.services.stt.transcribe_audio` to return a canned Indonesian transcript + duration, and patch the Anthropic client so `poll_summary_batch` parses a canned succeeded batch result with known token usage. Write `test_offline_session_full_pipeline`: drive `CompleteSessionView` (reuse test_session_execution.py's consented-session + `webm_audio_file` setup), assert the logbook reaches `ready_for_review`, POST approve as the owning lecturer, assert `status=approved`/`approved_by`, GET the student endpoint as the owning student and assert the summary is returned, and assert a non-owner student is blocked. Write `test_online_session_full_pipeline` (set `method='online'`, assert `source_mode='online'` and identical outcome). Write `test_disabled_pipeline_falls_back_to_manual` (`settings.STT_LLM_ENABLED=False` via the `settings` fixture; complete → failed + STT_DISABLED log → manual-notes POST → approved-equivalent → student sees it). Write `test_stt_failure_logs_and_falls_back` (patch transcribe_audio to raise → failed + STT_FAILED). Use the `settings` pytest fixture to toggle `STT_LLM_ENABLED` per test.
  </action>
  <verify>
    <automated>cd backend && pytest apps/logbook/tests/test_logbook_integration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All four integration tests pass in eager mode with mocked STT/LLM (no real model, no network).
    - Offline and online tests both reach `ready_for_review` then `approved` and expose the summary to the owning student only.
    - The disabled test proves the STT-07 manual-notes fallback works end-to-end (failed → manual → student-visible).
    - `grep -c "source_mode" backend/apps/logbook/tests/test_logbook_integration.py` is ≥1 (online path asserts source_mode).
  </acceptance_criteria>
  <done>The full pipeline is proven end-to-end for offline and online sessions plus the disabled graceful-degradation path, closing STT-01..07.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Online-session lecturer-mic-only recording test (VIDEO-02)</name>
  <files>backend/apps/bimbingan/tests/test_session_recording.py</files>
  <read_first>
    - backend/apps/bimbingan/tests/test_session_execution.py (the existing recording-upload test coverage for CompleteSessionView — the consent-gated multipart audio path to reuse; if test_session_recording.py does not exist yet, create it alongside)
    - .planning/phases/06-stt-ai-summarization-logbook/06-CONTEXT.md (D-15/D-17: online sessions record lecturer-mic-only via the existing useMediaRecorder flow — VIDEO-02 requires NO new capture code, only confirmation the same flow feeds SessionRecording → STT for online)
    - backend/conftest.py (webm_audio_file, advisee_student, lecturer_user fixtures)
  </read_first>
  <behavior>
    - An online (`method='online'`) consented session completed with a `webm_audio_file` upload produces a `SessionRecording` linked to the session AND a `SessionLogbook` with `source_mode='online'`, using the exact same `CompleteSessionView` path as an offline session.
    - No online-specific capture branch exists — the test asserts the online recording is created identically to the offline case (VIDEO-02 satisfied by the existing flow, D-17).
  </behavior>
  <action>
    Add `test_online_session_lecturer_mic_only_recording` to `test_session_recording.py` (create the file if absent, mirroring test_session_execution.py's structure): set up an IN_PROGRESS online session with consent recorded, POST to `CompleteSessionView` with the `webm_audio_file` multipart upload, and assert a `SessionRecording` is created and a `SessionLogbook` with `source_mode='online'` exists — proving the online path reuses the offline recording flow with no new capture code (VIDEO-02/D-17). Do not add any dual-audio assertions (D-16 cancelled).
  </action>
  <verify>
    <automated>cd backend && pytest apps/bimbingan/tests/test_session_recording.py -q -k online && pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - The online-recording test passes and asserts both `SessionRecording` creation and `SessionLogbook.source_mode == 'online'`.
    - The FULL backend suite passes (`pytest -q`) — no regression across Phases 1-6.
    - `grep -c "source_mode.*online\|method.*online" backend/apps/bimbingan/tests/test_session_recording.py` is ≥1.
  </acceptance_criteria>
  <done>VIDEO-02 is verified: online sessions record lecturer-mic-only via the unchanged flow and enter the same pipeline as offline sessions; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness | Verification only — no new production trust surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-22 | Repudiation | untested seam masking a silent pipeline break | mitigate | End-to-end tests assert every state transition and the SystemLog fallback trail across the full pipeline, closing the gap between unit tests and real behavior |
| T-06-23 | Tampering | disabled-path leaking partial/unapproved data to students | mitigate | The disabled/failure tests assert students see content only after the manual-notes approval, never the failed intermediate state |
</threat_model>

<verification>
- Offline + online + disabled + STT-failure paths all pass in eager mode with mocks.
- VIDEO-02 online-recording test asserts the reused flow + source_mode.
- Full backend suite green (no Phase 1-6 regression).
</verification>

<success_criteria>
The phase is proven end-to-end: both session modes complete the STT→LLM→approve→student-visible pipeline, the graceful-degradation fallback works when the pipeline is disabled or fails, and VIDEO-02 is confirmed to need no new capture code — with the entire backend suite green.
</success_criteria>

<output>
Create `.planning/phases/06-stt-ai-summarization-logbook/06-09-SUMMARY.md` when done.
</output>
